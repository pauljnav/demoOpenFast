# New Project

### add
`main.tf` for the Azure configuration. It creates a Resource Group, VNet, Public IP, and a Standard_D6s_v3 VM (6 vCPUs) running Ubuntu 20.04.

It also uses the install_conda_openfast.sh script to bootstrap the VM.